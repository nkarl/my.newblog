{
    "name": "halogen-new-blog",
    "version": "0.0.1.0",
    "scripts": {
        "clean": "rm -rf output .spago dist/*.js .cache",
        "build": "spago build",
        "test": "spago test",
        "serve": "spago build && esbuild --bundle index.js --outfile=dist/index.js --servedir=dist",
        "bundle": "spago build && esbuild --bundle index.js --minify --outfile=dist/index.js"
    },
    "author": "Charles Lambert Nguyen",
    "license": "ISC",
    "description": "A PureScript and Halogen blog.",
    "packageManager": "pnpm@11.22.0",
    "dependencies": {
        "@wcj/markdown-to-html": "^3.0.4",
        "esbuild": "^0.25.1"
    },
    "devDependencies": {
        "purescript": "^0.15.16",
        "spago": "^1.0.4"
    }
}
